# Montecarlo


---

# 📄 Паспорт Проекта: Децентрализованная Сеть Финансовых Вычислений

## 1. О чем проект и для чего он (Mission Statement)

Суть:

Мы строим распределенный суперкомпьютер, объединяющий тысячи простаивающих потребительских ноутбуков и ПК (Crowd-Sourced Compute) для выполнения тяжелых математических симуляций (Monte Carlo).

Проблема:

Финансовым компаниям нужно просчитывать риски (Value at Risk, CVA) быстро. Аренда суперкомпьютеров дорога, а классические облака (AWS) медленно масштабируются: чтобы запустить 1000 ядер на 10 минут, нужно потратить время на их настройку.

Решение:

Мы предоставляем доступ к пулу из тысяч уже запущенных устройств. Мы дробим одну огромную задачу на тысячи мелких, отправляем их на домашние компьютеры пользователей, и через 5–10 минут собираем результат.

**Целевая Аудитория (ЦУ):**

1. **Proprietary Trading Firms (Проп-трейдинг):** Компании, торгующие на свои деньги, которым критически важна скорость переоценки портфеля внутри дня.
    
2. **Boutique Hedge Funds ($10M–$100M AUM):** Фонды, которым нужны сложные модели, но содержать свой дата-центр нерентабельно.
    
3. **DeFi & Crypto Analytics:** Протоколы, рассчитывающие риски ликвидаций и стресс-тесты токеномики.
    
4. **InsurTech:** Стартапы, рассчитывающие актуарные модели.
    

---

## 2. В чем наше преимущество децентрализации (Value Proposition)

Почему клиент выберет нас, а не AWS или Azure?

1. **Скорость (Time-to-Result):**
    
    - _AWS:_ Чтобы арендовать 500 серверов на 10 минут, нужно ждать их инициализации.
        
    - _Мы:_ У нас уже есть 5000 активных воркеров в ожидании задачи. Мы включаем **мгновенный параллелизм**. То, что считается 10 часов на одном ядре, мы делаем за **6 минут** на 100 ядрах.
        
2. **Экономическая эффективность:**
    
    - Мы используем ресурс, который для владельца ничего не стоит (idle time). Это позволяет нам держать высокую маржу, предлагая клиенту цену ниже, чем On-Demand инстансы облаков.
        
3. **Масштабируемость "по запросу" (Burst Capacity):**
    
    - Идеально подходит для задач с "рваной" нагрузкой (например, расчет рисков 1 раз в день перед открытием биржи).
        

---

## 3. Техническая архитектура и работа ПО

Система состоит из трех элементов: **Клиент**, **Мастер (Центр)**, **Воркер (Агент)**.

### А. Схема Потоков Данных

1. **Вход (Input):** Клиент отправляет JSON (1–5 КБ) с параметрами модели (например, `Black-Scholes`, волатильность `20%`, итераций `100 млн`).
    
    - _Данные обезличены:_ Воркер видит только цифры, он не знает, акции какой компании считает.
        
2. **Распределение:** Мастер дробит задачу на 1000 подзадач (Job) по 100 тыс. итераций.
    
3. **Вычисление:** Агенты на ПК пользователей забирают задачи.
    
    - _Режим работы:_ Агент использует строго **25% CPU**, работая в фоне без тормозов для пользователя.
        
    - _Изоляция:_ Вычисления идут в изолированной среде (Sandbox), без доступа к файлам пользователя.
        
4. **Сборка (Output):** Агенты возвращают статистику (среднее, мин/макс, хеш). Мастер агрегирует результаты в один отчет.
    

### Б. Стек Технологий (MVP)

- **Master (Server):** Python (FastAPI) — для API. Redis — как сверхбыстрая очередь задач. PostgreSQL — для биллинга и юзеров.
    
- **Worker (Client):** Python (компилированный в .exe) или Go. Кроссплатформенный, легкий, с жестким лимитом ресурсов.
    
- **Protocol:** HTTPS (Long Polling или WebSocket) для связи.
    

---

## 4. Финансовый расчет (Unit Economics)

Расчет на **1 стандартный лот** (100 млн итераций симуляции).

### Расходы (COGS)

- **Выплаты воркерам:** Мы платим **$0.25** за час _верифицированного_ полезного времени CPU.
    
    - На 1 лот нужно ~10 часов машинного времени.
        
    - Итого выплаты: **$2.50**.
        
- **Инфраструктура (Мастер):** Амортизация сервера + трафик + верификация.
    
    - Итого инфра: **~$6.00**.
        
- **Всего расходы:** **~$8.50**.
    

### Доходы (Revenue)

- **Цена для клиента:** **$400.00** за лот.
    
    - Это дешевле, чем нанимать команду девопсов и арендовать On-Demand кластер в AWS.
        
    - Клиент платит премию за то, что получает результат _сейчас_.
        

### Итог

- **Валовая прибыль:** $391.50 с одного лота.
    
- **Маржинальность:** **~97%**.
    

---

## 5. Слабые места и их решение (Risk Mitigation)

Самые критичные риски, которые могут разрушить бизнес, и наши протоколы защиты.

### А. Доверие и Конфиденциальность (Trust)

- _Риск:_ Клиент боится, что "школьник Вася" украдет его торговую стратегию.
    
- _Решение:_ **Data Obfuscation**. Мы передаем только математические примитивы. Воркер получает задачу `Calculate(A, B, C)`, где A, B, C — обезличенные числа. Восстановить из этого финансовую стратегию невозможно.
    

### Б. Читерство и Фрод (Verification)

- _Риск:_ Воркер модифицирует клиент, чтобы не считать CPU, а присылать случайные числа (`return random()`), получая деньги "за воздух".
    
- _Решение 1 (Статистика):_ **Z-Score проверка**. Результат каждой подзадачи проверяется на отклонение от среднего. Если результат вылетает за 3 сигмы — бан.
    
- _Решение 2 (Репликация):_ 5–10% задач дублируются на двух разных воркерах. Если их хеши не совпадают — оба перепроверяются третьим "арбитром".
    

### В. Нестабильность воркеров (Churn)

- _Риск:_ Пользователь выключил ноутбук посередине расчета.
    
- _Решение:_ **Over-provisioning**. Мастер отправляет одну задачу сразу троим воркерам. Результат принимается от первого, кто закончил. Остальные отменяются или используются для проверки. Это гарантирует SLA по времени.
    

---

## ⚙️ Архитектура Данных и Процесс Вычислений

В отличие от классических задач Big Data, наш проект работает по принципу **«Мало Данных — Много Вычислений»** (Compute-Heavy / Data-Light). Мы не передаем гигабайты информации, мы передаем математические инструкции.

### 1. Входные Данные (Input Payload)

Что мы получаем от Заказчика:

Клиент передает не историю сделок или персональные данные, а параметры математической модели. Это «инструкция», описывающая, как ведет себя актив, но не называющая его.

- **Формат:** Легковесный JSON-файл (размер: < 5 КБ).
    
- **Состав пакета:**
    
    - **ID Задачи:** Уникальный идентификатор.
        
    - **Тип Модели:** Указание алгоритма (например, `Black-Scholes`, `Heston Model`, `GARCH`).
        
    - **Математические Параметры:** Начальная цена (обфусцированная), волатильность (σ), дрифт (μ), временной горизонт (T), корреляционная матрица (для портфелей).
        
    - **Количество итераций:** Сколько сценариев нужно прогнать (например, 100 000 на одного воркера).
        

**Пример JSON (то, что видит воркер):**

JSON

```
{
  "job_id": "8f42-a1b9",
  "algorithm": "Geometric_Brownian_Motion",
  "params": {
    "S0": 100.0,       // Начальная точка (абстрактное число)
    "sigma": 0.25,     // Волатильность 25%
    "r": 0.05,         // Безрисковая ставка 5%
    "T": 1.0,          // Горизонт 1 год
    "steps": 252       // Торговых дней
  },
  "simulations_count": 50000
}
```

> **Ключевой момент безопасности:** Воркер видит только математику. Он не знает, симулирует ли он цену на нефть, акции Apple или курс Биткоина.

---

### 2. Процесс Исполнения (On-Device Execution)

Что происходит на устройстве Пользователя:

Агент получает JSON, создает изолированную среду (Sandbox) и запускает цикл генерации случайных чисел.

1. **Инициализация:** Выделяется оперативная память (RAM) только под текущие переменные (менее 100 МБ).
    
2. **Генерация (The Loop):** Процессор выполняет миллионы циклов:
    
    - Генерация случайного числа (Random Seed).
        
    - Применение формулы модели к текущей цене.
        
    - Запись финального результата сценария (например, "Цена через год = 98.5").
        
3. **Ресурсоемкость:**
    
    - **CPU:** Загрузка 25% (или 100% на одном ядре) — основная работа.
        
    - **Disk I/O:** 0% (ничего не пишется на диск).
        
    - **Network:** 0% (во время расчета интернет не нужен).
        

---

### 3. Выходные Данные (Output Result)

Что мы возвращаем Заказчику:

Чтобы не «забивать» канал связи передачей 50 000 чисел от каждого воркера, агент производит первичную агрегацию на месте. Мы возвращаем статистический слепок результата.

- **Формат:** JSON-файл (размер: < 2 КБ).
    
- **Состав ответа:**
    
    - **Mean (Среднее):** Ожидаемая цена актива.
        
    - **Std Dev (Стандартное отклонение):** Мера риска.
        
    - **VaR (Value at Risk):** Значения 5-го и 1-го перцентиля (худшие сценарии).
        
    - **Hash:** Криптографическая подпись результата для верификации честности вычислений.
        

**Пример ответа воркера:**

JSON

```
{
  "job_id": "8f42-a1b9",
  "worker_id": "node_771",
  "compute_time_ms": 4500,
  "result": {
    "mean": 105.42,
    "min": 65.10,
    "max": 180.20,
    "percentile_5": 82.00  // Риск-метрика
  },
  "integrity_hash": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
}
```

---

### 4. Итоговая сборка (Aggregation)

Роль Мастер-Сервера:

Мастер получает сотни таких маленьких JSON-ответов от разных воркеров. Поскольку выборки независимы, Мастер просто объединяет их (усредедняет средние, пересчитывает общую дисперсию) и выдает Клиенту один финальный отчет с высочайшей точностью.

### Почему это эффективно?

- **Трафик:** Общий обмен данными < 10 КБ на задачу. Можно работать даже через 2G/Edge.
    
- **Скорость:** Передача данных занимает миллисекунды, всё время тратится именно на полезные вычисления.
    
- **Приватность:** Данные не покидают периметр в понятном для человека виде.
